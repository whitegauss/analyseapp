[pytest]
# Declare the import root explicitly. Collection currently resolves `app` only
# because tests/ is a package and pytest inserts this directory as rootdir; say
# so out loud rather than relying on that inference.
pythonpath = .
testpaths = tests

# Coverage is opt-in (scripts/test.sh --cov) so the default run stays fast.
